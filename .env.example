# ── Servidor ────────────────────────────────
PORT=3000
NODE_ENV=development

# ── Banco de Dados MySQL ─────────────────────
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=gamevault

# ── JWT ──────────────────────────────────────
# IMPORTANTE: troque por uma string longa e aleatória em produção
JWT_SECRET=gamevault_secret_change_me_in_production_2026
JWT_EXPIRES_IN=7d

# ── CORS ─────────────────────────────────────
FRONTEND_URL=http://localhost:3000
